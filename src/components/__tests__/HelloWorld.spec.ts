// import { describe, it, expect } from 'vitest'

// import { mount } from '@vue/test-utils'

// describe('Login', () => {
//   it('renders properly', () => {
//     const wrapper = mount(LoginScreen, { props: { msg: 'Hello Vitest' } })
//     expect(wrapper.text()).toContain('Hello Vitest')
//   })
// })
